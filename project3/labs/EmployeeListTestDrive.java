/** This is the main method for the EmployeeList class. It will instantiate a
 *  new EmployeeList object and call its run() method.
 *
 *  @author Elspeth Stalter-Clouse
 */
public class EmployeeListTestDrive {
    /** Here's the main method:
     *  @param args
     */
    public static void main(String[] args) {
        // instantiate a new EmployeeList object
        EmployeeList employeeList = new EmployeeList();

        // call its run() method
        employeeList.run();
    }
}
